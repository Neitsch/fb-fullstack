language: generic

before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash
  - export PATH=$HOME/.yarn/bin:$PATH
cache:
  yarn: true

script: yarn run build

after_success:
- bash ./deploy.sh
