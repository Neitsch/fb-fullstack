sudo: false

language: node_js

node_js:
  - "6"

before_install:
  - npm install -g yarn
  - npm install
cache:
  yarn: true
  directories:
    - node_modules

script: npm run generate

after_success:
  - bash ./deploy.sh
